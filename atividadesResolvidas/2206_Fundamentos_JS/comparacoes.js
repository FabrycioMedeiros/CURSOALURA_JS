// == (comparação implicita)

const numero = 5;
const texto = "5";

console.log(numero === texto)

// typeof
console.log(typeof numero)
console.log(typeof texto)

// = atribui
// == só compara valores
// === compara valor e tipo de dado(por boas praticas utiliza este)

// conversão explicita
Number()
String()

// Outros tipos de operadores
/* Operador de adição " + "
   Operador de subtração " - " 
   Operador de divisão " / "
   Operador de multiplicação " * "

   Operador OU || retorna true caso condição seja valida
   Operador E && retorna true somente se todas  forem validas
   Operadores não iguais e extritamente não iguais != e !==, utilizados da mesma forma que na comparação, podem retornar true ou false
   Para buscar mais conhecimento, visitar documentação MDN em https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Expressions_and_operators*/
